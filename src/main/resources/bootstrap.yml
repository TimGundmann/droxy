server:
  port: 8765

spring:
  profiles:
    active:
    - prod
  application:
    name: zuul
  cloud:
    config:
      uri: https://www.gundmann.dk/config

management:
    endpoints:
     web:
        exposure:
            include: '*'
    server:
        port: 8764
